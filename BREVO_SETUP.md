# 農業AI通信 — GASからBrevoで予約配信

実装日: 2026-09-10。既存のDiscord → GAS → 原稿作成シートを維持し、テストメールと本配信をBrevo経由に変更します。

## 最初の設定

1. Brevoで `noujoujin.com` の送信ドメイン認証を完了します。Brevoが提示するDNSレコードをドメイン管理サービスに追加してください。既存メール用のMXレコードを変更する必要はありません。
2. Brevoで `yuichiro.kai@noujoujin.com` を送信者として登録・認証します。別アドレスを使う場合は、下記 `BREVO_SENDER_EMAIL` に設定します。返信先とテスト宛先は従来のOWNER_EMAILです。
3. GASの「プロジェクトの設定 → スクリプトプロパティ」に `BREVO_API_KEY` を設定します。コード・Git・チャットにAPIキーを貼らないでください。
4. エディタから `checkBrevoConnection` を実行します。接続・送信者認証の確認だけで、メールは送信しません。
5. `setupBrevoIntegration` を実行します。Brevoフォルダ、スプシの「Brevo配信管理」、5分間隔のGASトリガーを作り、連携を有効にします。読者登録・メール送信は行いません。
6. スプシを再読み込みし、原稿を作成して「1.5. 修正後にテスト送信（自分のみ）」で実際のメールを確認します。その後B3に未来の日時を入力して「3. 送信予約を設定する」を実行します。

`checkBrevoConnection` はDNS全レコードの検証やマーケティング配信の審査完了までは保証しません。Brevo画面上のドメイン認証・アカウント有効化も確認してください。設定が揃うまでは本番配信を有効化しません。

| プロパティ | 用途 |
|---|---|
| BREVO_API_KEY | 必須。Brevo APIキー |
| BREVO_FOLDER_ID | 初期設定が自動作成・保存。予約ごとのリストを格納 |
| BREVO_ENABLED | 初期設定完了時にtrue。falseならワーカー停止 |
| BREVO_SENDER_EMAIL | 任意。既定はyuichiro.kai@noujoujin.com |
| BREVO_MAX_RECIPIENTS | 任意。既定290名、1〜1000。1キャンペーンの上限であり、日次残量ではありません |

無料枠300通/日を使う場合、テスト配信や他のキャンペーンも合算されます。290名の既定値は余裕を持たせたものですが、1日2回配信できるという意味ではありません。Brevo側の残量超過は配信管理に記録され、自動全件再送はしません。

## 日常の操作

1. Discordから記事を反映し、従来どおりGASで原稿を作成。
2. B1（件名）・B2（HTML本文）・B4（X投稿案）を確認。テストメールもBrevo経由。
3. B3に日時を入力し、メニュー「3」で予約。
4. **予約時点の原稿と宛先を固定保存**。原稿を修正したら再度予約してください。再予約は未送信のGAS予約を置き換えます。
5. GASが読者をBrevoへ同期し、予約時刻以降にキャンペーンを配信。Brevo受付と配信処理完了を区別し、完了確認後にアーカイブします。

従来の月・水・金の原稿作成トリガーは維持しています。自動原稿作成→テスト成功後の当日15時予約も同じ経路を使います。本日15時を過ぎている場合は手動で未来の日時を設定してください。

トリガーは指定時刻ぴったりの到達を保証しません。読者の同期が長い場合は次の5分間隔の実行で続きを処理します。初回は余裕を持って30分以上先を予約してください。「即時配信」も準備完了後、タイマー経由で配信します。

## 読者・配信停止

- スプシの配信リストを予約時に保存し、予約ごとのBrevoリストに同期します。新規読者は次の予約から対象です。
- アドレスは前後空白を除き小文字化・重複除去します。不正なアドレスや上限超過は配信前に停止します。
- 予約後にスプシから削除された読者は、送信直前にBrevoの対象リストから除外します。既存Googleフォームの削除処理も維持します。
- Brevoの配信停止リンクを本番メールに追加します。Brevoで停止済みの読者をコードから再購読させません。
- Brevo側の停止はスプシの行削除には自動反映しません。Brevoの抑止情報を正とし、Marketing Campaignsが配信対象から除外します。
- 専用の予約リストは監査用に残します。送信者・読者・原稿をBrevo管理画面で直接変更するとGASの固定原稿とは異なるため、通常の修正はスプシで再予約してください。

## 状態の見方

| 状態 | 意味・対応 |
|---|---|
| QUEUED / PREPARING | 予約済み・読者同期中 |
| READY | 予約時刻待ち |
| CREATING | キャンペーン作成中。中断された場合はREVIEWへ |
| SENDING | 配信要求を記録済み。結果不明・API拒否の場合も自動再送しない |
| ACCEPTED | Brevoが受付済み。到達完了ではない |
| SENT | Brevoのsent状態を確認してアーカイブ済み。各読者への到達はBrevoで確認 |
| FAILED | 送信要求前の失敗。原因修正後、キャンセルして再予約可能 |
| REVIEW / SUSPENDED | Brevo画面で確認が必要。別記事の原稿作成は止めない |
| CANCELLED | GASの予約取消済み |

「6. Brevo配信状況を更新」は既知のキャンペーンIDについて状態を読み取り、送信要求を出しません。SENDINGでBrevoがdraftのままの場合、残量・審査・認証を確認してください。確認待ちの管理データを手動で削除して再送しないでください。配信結果を照合してから個別に復旧します。

「4. 予約をキャンセル」は未送信のGAS予約のみ取り消します。Brevoへ受付済みのものは取り消さないため、Brevo管理画面で確認してください。

旧 `AI_GUIDE_SEND_IN_FLIGHT` は削除しません。Gmail部分送信と同じ記事だけを予約禁止にし、新しい記事は作成・予約できます。旧Gmail配信の未送信者を自動再送する実装ではありません。

## 配置と検証

- `auto-mail.gs`: 既存原稿作成・メニュー・予約入口。現行GASの変更を維持した差分。
- `BrevoMail.gs`: API、予約データ、状態確認、二重送信防止。本番GASではauto-mail.gs末尾に同内容を統合しても動作します。**両方重複して配置しないでください。**
- `BrevoSetup.gs`: 初期設定だけを実行する入口。エディタでこのファイルを選んで `initializeBrevoAccount` を実行しても、手順5と同じ設定を行います。
- `scripts/build-brevo-mail.js`: 現行GASのauto-mailを入力に、必要な関数のみ差し替える移行用ツール。`node scripts/build-brevo-mail.js <現行コード.gs> <出力.gs>`。BrevoMail.gsは別途配置。
- `test/brevo-mail.test.js`: 外部送信なしのAPI・GASモックテスト。予約原稿保持、キャンセル、タイムアウト、残量不足、重複防止、配信停止、宛先上限、アーカイブ復旧などを検証。

実アカウントでの接続、Brevo審査・ドメイン、実配送は認証情報設定後の確認が必要です。

## 公式資料

- [キャンペーン作成](https://developers.brevo.com/reference/create-email-campaign)
- [配信要求](https://developers.brevo.com/reference/send-email-campaign-now)
- [連絡先の追加・更新](https://developers.brevo.com/reference/create-contact)
- [キャンペーン状態取得](https://developers.brevo.com/reference/get-email-campaign)
- [Brevo料金・無料枠](https://help.brevo.com/hc/en-us/articles/208589409-About-Brevo-s-pricing-plans)
