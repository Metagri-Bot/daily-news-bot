# 農家・食品パートナー案件レーダー

農家との継続的な関係づくり、AI/DX・EC・販促支援、農業AI通信の実証記事につながる副業・業務委託案件を通知します。既存の官公庁公募とは別モジュール・別履歴です。

## 運用

- 平日8:20 JST。`FARM_PARTNER_CRON` で変更。`DISABLE_FARM_PARTNER_RADAR=true` で停止。
- 投稿先は `FARM_PARTNER_CHANNEL_ID` → `PUBLIC_OPPORTUNITY_CHANNEL_ID` → `NEWS_CHANNEL_ID`。
- 既定65点以上、最大5件。該当なし／変更なしは投稿しません。
- 必要権限はチャンネル閲覧・メッセージ送信・埋め込み・メッセージ履歴閲覧。
- 外部LLMは使いません。追加のAI API課金なし。記事企画はルールによる提案であり、募集元がAI活用や公開を認めたという意味ではありません。

## 収集範囲と限界

Skill Shiftの公開画面が使う `/api/v1/jobs` を、`sort=new_arrival`・100件×4ページ取得。関連候補の詳細は最大40件。`is_applicable=true` かつ `expire/disabled/suspended=false` の詳細だけを受付可能とします。認証不要の公開情報のみ。非公開・安定保証のないAPIのため、応答構造が変わったらログと監視Webhookで異常を通知します。

ふるさと兼業は公開一覧の先頭3ページ（現状30件）、詳細最大20件。募集終了文言、募集終了日を検査し、期限が未来で応募操作が存在するときだけ受付可能にします。

YOSOMON! は[募集中の公開一覧](https://yosomon.etic.or.jp/projects?q%5Bonly_recruiting%5D=true)の先頭3ページ、詳細最大30件を監視します。詳細の「募集中」表示とエントリー導線を両方確認します。イベント・説明会の告知は通知しません。謝礼、勤務スタイル、必須スキルは詳細ページから取得します。食分野の事業者も採点しますが、農家との直接接点がない案件は低めに評価します。

状態不明は候補として残しても通知しません。最新掲載範囲の監視であり、全アーカイブ網羅・即時検知・募集終了後の通知取り消しを保証するものではありません。締切リマインドは本機能の対象外です。

1リクエスト25秒以内、取得間隔250ms。部分失敗は正常取得分を処理し、エラーを `lastRun` とログへ保存して既存監視Webhookにも通知。失敗を「候補0件」と混同しません。

## 採点（自動仮評価）

農家との直接接点30、スキル適合30、記事化の題材20、実行可能性10、継続性10。案件本文の会社紹介だけでなく、案件名・募集条件に依頼職種があるかを確認します。CAD・Shopify・輸出入・食品技術等は最大59点、受注までの法人営業経験が必要な案件は最大64点に抑えます。これらの経験が確認できた場合は評価関数を調整してください。印刷物制作は通知しますが、制作実績確認を表示します。

自動点数は選考通過確率ではなく一次選別です。人が付ける最終順位では、農家との関係の深さ、契約工数、既存案件との両立も加味します。

## 重複と再起動

`state/farm-partner-radar.json` に案件IDと、件名・本文・条件・働き方・報酬・期限・受付状態のハッシュを保存。応募人数や取得日時のみの変化は再通知しません。送信成功後に1件ずつ一時ファイル経由で保存します。保存失敗は処理停止、履歴破損は配信停止。同一プロセスの多重実行も抑止します。

Dockerの既存 `bot-state` ボリュームで履歴を保持します。起動後の配信前に同じBotが送った直近100メッセージのフッターも確認し、送信直後にプロセスが落ちた場合を補います。Discordのnonceも併用。ただし履歴ボリューム削除かつ100メッセージより古い投稿は復元できません。複数Botプロセス同時稼働は対象外です。

## 手動確認

```sh
node scripts/farm-partner-radar.js --output tmp/farm-radar-dry.json
node --test test/farm-partner-radar.test.js
node scripts/farm-partner-radar.js --send
```

既定はdry-runで、Discord送信・配信履歴更新はしません。`--send` のみ実送信。履歴パスはBotと共通です。`--output` は指定した場所へ調査結果JSONを保存します。`index.js` 全体をローカル起動すると既存ニュースの二重稼働になるため、検証には上記専用CLIを使用します。

GitHub mainへのpushで既存Actionsがデプロイします。追加Secretsは任意で、空なら上記既定値で稼働します。通知した案件への応募・連絡や記事公開は行いません。

本番で今すぐ実行する場合はDeploy Daily News Botの手動実行で `send_farm_partner_once=true` を指定できます。デプロイ後にコンテナ内の専用CLIを1回実行し、定期実行と同じボリュームに配信履歴を保存します。定期実行時間帯を避けて使用してください。
